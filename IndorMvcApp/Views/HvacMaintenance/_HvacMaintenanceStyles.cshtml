@await Html.PartialAsync("../Cleaning/_CleaningStyles")
<style>
    .hvac-stepper { display: flex; align-items: center; gap: 0; margin-bottom: 18px; }
    .hvac-stepper .line { flex: 1; height: 2px; background: #E5E7EB; }
    .hvac-stepper .line.done { background: #0066CC; }
    .hvac-step-dot { width: 28px; height: 28px; border-radius: 50%; border: 2px solid #CBD5E1; color: #94A3B8; background: #FFF; display: flex; align-items: center; justify-content: center; font-size: 0.78rem; font-weight: 700; flex-shrink: 0; }
    .hvac-step-dot.active, .hvac-step-dot.done { border-color: #0066CC; background: #0066CC; color: #FFF; }
    .hvac-hero-icon { position: absolute; top: 10px; left: 10px; width: 34px; height: 34px; border-radius: 10px; background: #0066CC; color: #FFF; display: flex; align-items: center; justify-content: center; font-size: 0.95rem; box-shadow: 0 4px 10px rgba(0,102,204,0.25); }
    .hero-wrap { position: relative; margin-bottom: 16px; }
    .hero-wrap .hero-img { margin-bottom: 0; }
    .includes-box { background: #F9FAFB; border: 1px solid #E5E7EB; border-radius: 14px; padding: 12px 14px; margin-bottom: 14px; }
    .includes-box .includes-label { font-size: 0.65rem; font-weight: 700; letter-spacing: .8px; text-transform: uppercase; color: #6B7280; margin-bottom: 8px; }
    .includes-box li { list-style: none; display: flex; align-items: center; gap: 8px; font-size: 0.82rem; font-weight: 600; padding: 4px 0; }
    .includes-box li i { color: #0066CC; font-size: 0.72rem; }
    .preview-box { background: #FFF; border: 1px solid #E5E7EB; border-radius: 14px; padding: 12px 14px; margin-bottom: 14px; }
    .preview-box .preview-label { font-size: 0.82rem; font-weight: 700; margin-bottom: 8px; }
    .preview-row { display: flex; align-items: center; justify-content: space-between; gap: 10px; padding: 10px 0; border-bottom: 1px solid #F1F5F9; font-size: 0.8rem; font-weight: 600; }
    .preview-row:last-child { border-bottom: none; }
    .preview-row .left { display: flex; align-items: center; gap: 10px; }
    .preview-row .left i { color: #0066CC; width: 16px; }
    .preview-row .right { color: #94A3B8; font-size: 0.72rem; }
    .field-card { background: #FFF; border: 1px solid #E5E7EB; border-radius: 14px; padding: 14px; margin-bottom: 12px; }
    .field-card .field-head { display: flex; align-items: center; gap: 10px; margin-bottom: 10px; font-size: 0.82rem; font-weight: 700; }
    .field-card .field-head i { color: #0066CC; width: 16px; }
    .field-card input, .field-card textarea { width: 100%; border: 1.5px solid #E5E7EB; border-radius: 12px; padding: 12px 14px; font-family: inherit; font-size: 0.88rem; background: #F9FAFB; }
    .field-card input[type="date"] {
        min-height: 48px; color: #0A2540; cursor: pointer;
        -webkit-appearance: none; appearance: none;
    }
    .field-card input[type="date"]:disabled { opacity: 0.55; cursor: not-allowed; }
    .field-card input[type="date"]::-webkit-calendar-picker-indicator {
        cursor: pointer; opacity: 0.75; padding: 4px;
    }
    .field-hint { display: block; margin-top: 8px; font-size: 0.72rem; color: #64748B; font-weight: 500; }
    .field-card textarea { min-height: 90px; resize: vertical; }
    .toggle-row { display: flex; align-items: center; justify-content: space-between; gap: 12px; }
    .toggle-row span { font-size: 0.8rem; font-weight: 600; color: #64748B; }
    .toggle-switch { position: relative; width: 44px; height: 26px; flex-shrink: 0; }
    .toggle-switch input { opacity: 0; width: 0; height: 0; }
    .toggle-slider { position: absolute; inset: 0; background: #CBD5E1; border-radius: 999px; cursor: pointer; transition: .2s; }
    .toggle-slider:before { content: ""; position: absolute; width: 20px; height: 20px; left: 3px; top: 3px; background: #FFF; border-radius: 50%; transition: .2s; }
    .toggle-switch input:checked + .toggle-slider { background: #0066CC; }
    .toggle-switch input:checked + .toggle-slider:before { transform: translateX(18px); }
    .link-inline { color: #0066CC; font-size: 0.78rem; font-weight: 700; background: none; border: none; padding: 0; cursor: pointer; }
    .upload-drop-wrap { position: relative; overflow: hidden; border-radius: 14px; }
    .upload-drop {
        display: flex; flex-direction: column; align-items: center; justify-content: center;
        border: 1.5px dashed #CBD5E1; border-radius: 14px; padding: 18px; text-align: center;
        color: #64748B; background: #FFF; cursor: pointer; min-height: 96px;
        position: relative; z-index: 1; touch-action: manipulation; -webkit-tap-highlight-color: transparent;
    }
    .upload-drop i { color: #0066CC; font-size: 1.1rem; margin-bottom: 6px; pointer-events: none; }
    .upload-drop strong { display: block; font-size: 0.82rem; margin-bottom: 4px; color: #334155; pointer-events: none; }
    .upload-drop span { font-size: 0.72rem; pointer-events: none; }
    .upload-input-hidden { display: none; }
    .upload-drop.has-files { border-color: #0066CC; background: rgba(0,102,204,0.06); }
    .upload-selected-list { margin-top: 10px; display: flex; flex-direction: column; gap: 6px; }
    .upload-selected-item {
        display: flex; align-items: center; gap: 8px; font-size: 0.78rem; color: #0A2540;
        background: #F8FAFC; border: 1px solid #E5E7EB; border-radius: 10px; padding: 8px 10px;
    }
    .upload-selected-item i { color: #0066CC; flex-shrink: 0; }
    .upload-selected-item span { flex: 1; min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
    .upload-preview-thumb { width: 40px; height: 40px; border-radius: 8px; object-fit: cover; flex-shrink: 0; }
    .upload-remove-btn {
        border: none; background: transparent; color: #94A3B8; padding: 4px; cursor: pointer;
        flex-shrink: 0; line-height: 1;
    }
    .time-grid-3 { display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; }
    .time-card { border: 1.5px solid #E5E7EB; background: #F9FAFB; border-radius: 14px; padding: 12px 8px; cursor: pointer; text-align: center; position: relative; min-height: 92px; }
    .time-card i.main { display: block; font-size: 1rem; color: #94A3B8; margin-bottom: 8px; }
    .time-card .label { display: block; font-size: 0.74rem; font-weight: 700; margin-bottom: 4px; }
    .time-card .sub { display: block; font-size: 0.68rem; color: #64748B; font-weight: 600; }
    .time-card .radio { position: absolute; top: 8px; right: 8px; width: 18px; height: 18px; border-radius: 50%; border: 2px solid #CBD5E1; }
    .time-card.active { border-color: #0066CC; background: rgba(0,102,204,0.08); color: #0066CC; }
    .time-card.active i.main { color: #0066CC; }
    .time-card.active .radio { border-color: #0066CC; background: #0066CC; box-shadow: inset 0 0 0 3px #FFF; }
    .service-type-row { display: flex; gap: 8px; flex-wrap: wrap; }
    .service-type-btn { border: 1.5px solid #E5E7EB; background: #F9FAFB; border-radius: 999px; padding: 10px 14px; font-weight: 600; font-size: 0.78rem; color: #64748B; cursor: pointer; display: inline-flex; align-items: center; gap: 6px; }
    .service-type-btn.active { border-color: #0066CC; color: #0066CC; background: rgba(0,102,204,0.08); }
    .contact-card { background: rgba(0,102,204,0.06); border: 1px solid rgba(0,102,204,0.15); border-radius: 14px; padding: 14px; margin-bottom: 14px; }
    .contact-card .row { display: flex; align-items: flex-start; gap: 10px; font-size: 0.82rem; font-weight: 600; margin-bottom: 8px; }
    .contact-card .row:last-child { margin-bottom: 0; }
    .contact-card .row i { color: #0066CC; margin-top: 2px; }
    .sticky-footer { position: sticky; bottom: 0; background: rgba(255,255,255,0.96); backdrop-filter: blur(6px); border-top: 1px solid #E5E7EB; padding-top: 12px; margin-top: 12px; }
    .sticky-footer-row { display: flex; align-items: center; justify-content: space-between; gap: 12px; margin-bottom: 10px; }
    .sticky-footer-row .price-label { font-size: 0.72rem; color: #64748B; font-weight: 600; display: flex; align-items: center; gap: 6px; }
    .sticky-footer-row .price-value { font-size: 1.2rem; font-weight: 800; color: #0066CC; }
    .success-wrap { text-align: center; margin-bottom: 16px; }
    .success-icon { width: 72px; height: 72px; border-radius: 50%; background: #0066CC; color: #FFF; display: flex; align-items: center; justify-content: center; font-size: 1.8rem; margin: 0 auto 12px; box-shadow: 0 8px 20px rgba(0,102,204,0.25); }
    .next-steps { background: #FFF; border: 1px solid #E5E7EB; border-radius: 16px; padding: 14px; margin-bottom: 14px; }
    .next-steps h4 { font-size: 0.82rem; font-weight: 700; margin-bottom: 10px; }
    .next-step-item { display: flex; align-items: flex-start; gap: 10px; font-size: 0.78rem; padding: 6px 0; line-height: 1.35; }
    .next-step-item i { color: #0066CC; margin-top: 2px; }
</style>
