@using IndorMvcApp.Localization
@{
    var stepModel = Model as IndorMvcApp.ViewModels.RealtorQuoteRequestStepViewModel;
    var pageTitle = stepModel?.Title ?? L["Request Quote"];
}
<!DOCTYPE html>
<html lang="@UiCulture.HtmlLang(L.CurrentCulture)">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <meta name="theme-color" content="#0066CC" />
    <title>@pageTitle - INDOR</title>
    @await Html.PartialAsync("_RealtorPortalHead")
</head>
<body class="rl-dash-page">
    <div class="rl-invite-shell">
        @if (stepModel != null)
        {
            @await Html.PartialAsync("_QuoteRequestStepper", stepModel)
        }
        @RenderBody()
    </div>
    @await Html.PartialAsync("~/Views/Realtor/_RealtorBottomNav.cshtml")
    @RenderSection("Scripts", required: false)
</body>
</html>
