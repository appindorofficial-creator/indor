@await Html.PartialAsync("../HvacMaintenance/_HvacMaintenanceStyles")
<style>
    .ep-title-row { display: flex; align-items: flex-start; justify-content: space-between; gap: 12px; margin-bottom: 8px; }
    .ep-title-row .left { flex: 1; }
    .ep-thumb { width: 72px; height: 72px; border-radius: 12px; object-fit: cover; flex-shrink: 0; border: 1px solid #E5E7EB; }
    .question-num { font-size: 0.72rem; font-weight: 800; color: #0066CC; margin-bottom: 6px; }
    .paint-age-grid { display: grid; grid-template-columns: repeat(5, 1fr); gap: 6px; }
    .paint-age-btn { border: 1.5px solid #E5E7EB; background: #FFF; border-radius: 12px; padding: 10px 4px; cursor: pointer; text-align: center; min-height: 84px; position: relative; }
    .paint-age-btn i { display: block; font-size: 0.95rem; color: #0066CC; margin-bottom: 6px; }
    .paint-age-btn span { font-size: 0.58rem; font-weight: 700; line-height: 1.25; display: block; }
    .paint-age-btn.active { border-color: #0066CC; background: rgba(0,102,204,0.08); color: #0066CC; }
    .surface-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; }
    .surface-card { border: 1.5px solid #E5E7EB; background: #FFF; border-radius: 14px; padding: 12px 8px; cursor: pointer; text-align: center; min-height: 92px; position: relative; }
    .surface-card i.main { display: block; font-size: 1rem; color: #94A3B8; margin-bottom: 8px; }
    .surface-card .label { font-size: 0.68rem; font-weight: 700; line-height: 1.25; }
    .surface-card .sub { font-size: 0.58rem; color: #64748B; margin-top: 4px; display: block; }
    .surface-card.active { border-color: #0066CC; background: rgba(0,102,204,0.08); color: #0066CC; }
    .surface-card.active i.main { color: #0066CC; }
    .color-grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; }
    .color-card { border: 1.5px solid #E5E7EB; background: #F9FAFB; border-radius: 14px; padding: 16px 10px; cursor: pointer; text-align: center; min-height: 88px; }
    .color-card i.main { display: block; font-size: 1.1rem; color: #94A3B8; margin-bottom: 8px; }
    .color-card .label { font-size: 0.82rem; font-weight: 700; }
    .color-card.active { border-color: #0066CC; background: rgba(0,102,204,0.08); color: #0066CC; }
    .color-card.active i.main { color: #0066CC; }
    .issue-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; }
    .issue-card { border: 1.5px solid #E5E7EB; background: #FFF; border-radius: 14px; padding: 12px 6px; cursor: pointer; text-align: center; min-height: 84px; position: relative; }
    .issue-card i.main { display: block; font-size: 0.95rem; color: #94A3B8; margin-bottom: 6px; }
    .issue-card .label { font-size: 0.64rem; font-weight: 700; line-height: 1.25; }
    .issue-card .check { position: absolute; top: 8px; right: 8px; width: 18px; height: 18px; border-radius: 50%; background: #0066CC; color: #FFF; display: none; align-items: center; justify-content: center; font-size: 0.6rem; }
    .issue-card.active { border-color: #0066CC; background: rgba(0,102,204,0.08); color: #0066CC; }
    .issue-card.active i.main { color: #0066CC; }
    .issue-card.active .check { display: flex; }
    .color-update-card { border: 1.5px solid #E5E7EB; background: #FFF; border-radius: 14px; padding: 14px; cursor: pointer; text-align: left; width: 100%; position: relative; margin-bottom: 8px; }
    .color-update-card .radio { position: absolute; top: 14px; right: 14px; width: 18px; height: 18px; border-radius: 50%; border: 2px solid #CBD5E1; }
    .color-update-card strong { display: block; font-size: 0.82rem; margin-bottom: 4px; }
    .color-update-card span { font-size: 0.72rem; color: #64748B; }
    .color-update-card.active { border-color: #0066CC; background: rgba(0,102,204,0.08); }
    .color-update-card.active .radio { border-color: #0066CC; background: #0066CC; box-shadow: inset 0 0 0 3px #FFF; }
    .stories-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; }
    .story-card { border: 1.5px solid #E5E7EB; background: #F9FAFB; border-radius: 14px; padding: 14px 8px; cursor: pointer; text-align: center; min-height: 88px; position: relative; }
    .story-card i.main { display: block; font-size: 1rem; color: #94A3B8; margin-bottom: 8px; }
    .story-card .label { font-size: 0.74rem; font-weight: 700; }
    .story-card.active { border-color: #0066CC; background: rgba(0,102,204,0.08); color: #0066CC; }
    .story-card.active i.main { color: #0066CC; }
    .photo-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px; }
    .photo-slot { text-align: center; }
    .photo-upload { width: 72px; height: 72px; border: 1.5px dashed #CBD5E1; border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 6px; cursor: pointer; background: #F9FAFB; overflow: hidden; }
    .photo-upload i { color: #0066CC; font-size: 1rem; }
    .photo-upload img { width: 100%; height: 100%; object-fit: cover; }
    .photo-slot span { font-size: 0.68rem; font-weight: 700; color: #64748B; }
    .review-so-far { background: rgba(0,102,204,0.06); border: 1px solid rgba(0,102,204,0.15); border-radius: 14px; padding: 14px; margin-bottom: 14px; }
    .review-so-far h4 { font-size: 0.82rem; font-weight: 700; margin-bottom: 10px; display: flex; align-items: center; gap: 8px; }
    .review-so-far h4 i { color: #0066CC; }
    .review-mini-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
    .review-mini { font-size: 0.72rem; }
    .review-mini i { color: #0066CC; margin-bottom: 4px; display: block; }
    .review-mini .lbl { color: #64748B; font-weight: 600; margin-bottom: 2px; }
    .review-mini .val { font-weight: 700; color: #0066CC; line-height: 1.35; }
    .why-list .item { display: flex; align-items: flex-start; gap: 10px; font-size: 0.78rem; padding: 8px 0; line-height: 1.35; border-bottom: 1px solid #F1F5F9; }
    .why-list .item:last-child { border-bottom: none; }
    .why-list .item i { color: #0066CC; margin-top: 2px; }
    .confirm-box { background: rgba(0,102,204,0.08); border: 1px solid rgba(0,102,204,0.15); border-radius: 14px; padding: 14px; margin-bottom: 14px; display: flex; gap: 12px; align-items: flex-start; }
    .confirm-box i { color: #0066CC; font-size: 1.1rem; margin-top: 2px; }
    .confirm-box strong { display: block; font-size: 0.88rem; margin-bottom: 4px; }
    .confirm-box p { font-size: 0.78rem; color: #64748B; margin: 0; line-height: 1.4; }
    .reminder-box { background: rgba(0,102,204,0.08); border: 1px solid rgba(0,102,204,0.15); border-radius: 14px; padding: 14px; margin-bottom: 14px; display: flex; gap: 10px; align-items: flex-start; font-size: 0.78rem; font-weight: 600; }
    .reminder-box i { color: #0066CC; margin-top: 2px; }
    .reminder-box span.sub { display: block; font-size: 0.72rem; color: #64748B; font-weight: 600; margin-top: 4px; }
    .segment-row { display: flex; gap: 8px; flex-wrap: wrap; }
    .segment-btn { border: 1.5px solid #E5E7EB; background: #F9FAFB; border-radius: 999px; padding: 10px 14px; font-weight: 600; font-size: 0.78rem; color: #64748B; cursor: pointer; display: inline-flex; align-items: center; gap: 6px; }
    .segment-btn.active { border-color: #0066CC; color: #0066CC; background: rgba(0,102,204,0.08); }
    .choice-grid-3 { display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; }
    .choice-card { border: 1.5px solid #E5E7EB; background: #F9FAFB; border-radius: 14px; padding: 14px 8px; cursor: pointer; text-align: center; position: relative; min-height: 84px; }
    .choice-card i.main { display: block; font-size: 1rem; color: #94A3B8; margin-bottom: 8px; }
    .choice-card .label { font-size: 0.72rem; font-weight: 700; line-height: 1.25; }
    .choice-card.active { border-color: #0066CC; background: rgba(0,102,204,0.08); color: #0066CC; }
    .choice-card.active i.main { color: #0066CC; }
    .timing-row { display: grid; grid-template-columns: 1fr; gap: 8px; }
    .timing-card { border: 1.5px solid #E5E7EB; background: #F9FAFB; border-radius: 14px; padding: 14px; cursor: pointer; text-align: left; position: relative; width: 100%; }
    .timing-card i.main { color: #94A3B8; margin-right: 8px; }
    .timing-card .label { font-size: 0.82rem; font-weight: 700; }
    .timing-card .radio { position: absolute; top: 14px; right: 14px; width: 18px; height: 18px; border-radius: 50%; border: 2px solid #CBD5E1; }
    .timing-card.active { border-color: #0066CC; background: rgba(0,102,204,0.08); color: #0066CC; }
    .timing-card.active i.main { color: #0066CC; }
    .timing-card.active .radio { border-color: #0066CC; background: #0066CC; box-shadow: inset 0 0 0 3px #FFF; }
</style>
