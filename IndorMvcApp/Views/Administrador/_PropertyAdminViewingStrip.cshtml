@model IndorMvcApp.ViewModels.PropertyAdministratorFlowPropertyViewModel
@{
    var allowSwitch = ViewBag.AllowPropertySwitch as bool? ?? true;
    var badgeLabel = ViewBag.BadgeLabel as string;
}
@await Component.InvokeAsync("PropertyAdminViewingStrip", new
{
    property = Model,
    badgeLabel,
    allowSwitch
})
