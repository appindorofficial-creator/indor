@using IndorMvcApp.Localization
@{
    var stepModel = Model as IndorMvcApp.ViewModels.RealtorPropertyFileStepViewModel;
    var pageTitle = stepModel?.Title ?? L["Create Property File"];
}
<!DOCTYPE html>
<html lang="@UiCulture.HtmlLang(L.CurrentCulture)">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <meta name="theme-color" content="#0066CC" />
    <title>@pageTitle - INDOR</title>
    @await Html.PartialAsync("_RealtorPortalHead")
</head>
<body class="rl-dash-page">
    <div class="rl-invite-shell">
        @if (stepModel != null)
        {
            @await Html.PartialAsync("_PropertyFileStepper", stepModel)
        }
        @RenderBody()
    </div>
    @await Html.PartialAsync("~/Views/Realtor/_RealtorBottomNav.cshtml")
    @RenderSection("Scripts", required: false)
    @await Html.PartialAsync("_PropertyFileI18n")
    <script src="~/js/realtor-property-file.js" asp-append-version="true"></script>
</body>
</html>
