@model int
@{
    var steps = new[] { "Details", "Safety", "Photos", "Review" };
}
<style>
    .ef-stepper { display: flex; gap: 4px; margin-bottom: 18px; overflow-x: auto; padding-bottom: 4px; }
    .ef-step { flex: 1; min-width: 48px; text-align: center; }
    .ef-step-dot {
        width: 28px; height: 28px; border-radius: 50%; margin: 0 auto 6px;
        display: flex; align-items: center; justify-content: center;
        font-size: 0.78rem; font-weight: 700; border: 2px solid #CBD5E1; color: #94A3B8; background: #FFF;
    }
    .ef-step.done .ef-step-dot { border-color: #0066CC; background: #0066CC; color: #FFF; }
    .ef-step.active .ef-step-dot { border-color: #0066CC; color: #0066CC; background: #FFF; }
    .ef-step-label { font-size: 0.62rem; font-weight: 600; color: #94A3B8; line-height: 1.2; }
    .ef-step.active .ef-step-label, .ef-step.done .ef-step-label { color: #0066CC; }
</style>
<div class="ef-stepper">
    @for (var i = 0; i < steps.Length; i++)
    {
        var stepNum = i + 1;
        var css = stepNum < Model ? "ef-step done" : stepNum == Model ? "ef-step active" : "ef-step";
        <div class="@css">
            <div class="ef-step-dot">
                @if (stepNum < Model)
                {
                    <i class="fas fa-check" style="font-size:0.7rem;"></i>
                }
                else
                {
                    @stepNum
                }
            </div>
            <div class="ef-step-label">@steps[i]</div>
        </div>
    }
</div>
