@model int
@{
    var steps = new[] { "Details", "Property", "Photos", "Review" };
}
<style>
    .wi-stepper { display: flex; gap: 6px; margin-bottom: 18px; overflow-x: auto; padding-bottom: 4px; }
    .wi-step { flex: 1; min-width: 52px; text-align: center; }
    .wi-step-dot {
        width: 28px; height: 28px; border-radius: 50%; margin: 0 auto 6px;
        display: flex; align-items: center; justify-content: center;
        font-size: 0.78rem; font-weight: 700; border: 2px solid #CBD5E1; color: #94A3B8; background: #FFF;
    }
    .wi-step.done .wi-step-dot { border-color: #0066CC; background: #0066CC; color: #FFF; }
    .wi-step.active .wi-step-dot { border-color: #0066CC; color: #0066CC; background: #FFF; }
    .wi-step-label { font-size: 0.68rem; font-weight: 600; color: #94A3B8; line-height: 1.2; }
    .wi-step.active .wi-step-label, .wi-step.done .wi-step-label { color: #0066CC; }
</style>
<div class="wi-stepper">
    @for (var i = 0; i < steps.Length; i++)
    {
        var stepNum = i + 1;
        var css = stepNum < Model ? "wi-step done" : stepNum == Model ? "wi-step active" : "wi-step";
        <div class="@css">
            <div class="wi-step-dot">
                @if (stepNum < Model)
                {
                    <i class="fas fa-check" style="font-size:0.7rem;"></i>
                }
                else
                {
                    @stepNum
                }
            </div>
            <div class="wi-step-label">@steps[i]</div>
        </div>
    }
</div>
