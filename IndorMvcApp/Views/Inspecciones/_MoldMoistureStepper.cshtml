@model int
@{
    var steps = new[] { "Concern", "Property", "Photos", "Confirm" };
}
<style>
    .mold-stepper { display: flex; gap: 6px; margin-bottom: 18px; overflow-x: auto; padding-bottom: 4px; }
    .mold-step { flex: 1; min-width: 52px; text-align: center; }
    .mold-step-dot {
        width: 28px; height: 28px; border-radius: 50%; margin: 0 auto 6px;
        display: flex; align-items: center; justify-content: center;
        font-size: 0.78rem; font-weight: 700; border: 2px solid #CBD5E1; color: #94A3B8; background: #FFF;
    }
    .mold-step.done .mold-step-dot { border-color: #0066CC; background: #0066CC; color: #FFF; }
    .mold-step.active .mold-step-dot { border-color: #0066CC; color: #0066CC; background: #FFF; }
    .mold-step-label { font-size: 0.68rem; font-weight: 600; color: #94A3B8; line-height: 1.2; }
    .mold-step.active .mold-step-label, .mold-step.done .mold-step-label { color: #0066CC; }
</style>
<div class="mold-stepper">
    @for (var i = 0; i < steps.Length; i++)
    {
        var stepNum = i + 1;
        var css = stepNum < Model ? "mold-step done" : stepNum == Model ? "mold-step active" : "mold-step";
        <div class="@css">
            <div class="mold-step-dot">
                @if (stepNum < Model)
                {
                    <i class="fas fa-check" style="font-size:0.7rem;"></i>
                }
                else
                {
                    @stepNum
                }
            </div>
            <div class="mold-step-label">@steps[i]</div>
        </div>
    }
</div>
