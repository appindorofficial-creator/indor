@model int
@{
    var steps = new[] { "Describe", "Photos", "Review" };
}
<style>
    .etd-stepper { display: flex; gap: 4px; margin-bottom: 18px; overflow-x: auto; padding-bottom: 4px; }
    .etd-step { flex: 1; min-width: 56px; text-align: center; }
    .etd-step-dot {
        width: 28px; height: 28px; border-radius: 50%; margin: 0 auto 6px;
        display: flex; align-items: center; justify-content: center;
        font-size: 0.78rem; font-weight: 700; border: 2px solid #CBD5E1; color: #94A3B8; background: #FFF;
    }
    .etd-step.done .etd-step-dot { border-color: #0066CC; background: #0066CC; color: #FFF; }
    .etd-step.active .etd-step-dot { border-color: #0066CC; color: #0066CC; background: #FFF; }
    .etd-step-label { font-size: 0.62rem; font-weight: 600; color: #94A3B8; line-height: 1.2; }
    .etd-step.active .etd-step-label, .etd-step.done .etd-step-label { color: #0066CC; }
</style>
<div class="etd-stepper">
    @for (var i = 0; i < steps.Length; i++)
    {
        var stepNum = i + 1;
        var css = stepNum < Model ? "etd-step done" : stepNum == Model ? "etd-step active" : "etd-step";
        <div class="@css">
            <div class="etd-step-dot">
                @if (stepNum < Model)
                {
                    <i class="fas fa-check" style="font-size:0.7rem;"></i>
                }
                else
                {
                    @stepNum
                }
            </div>
            <div class="etd-step-label">@steps[i]</div>
        </div>
    }
</div>
