@model int
@{
    var steps = new[] { "Problem", "Details", "Review" };
}
<style>
    .erl-stepper { display: flex; gap: 4px; margin-bottom: 18px; overflow-x: auto; padding-bottom: 4px; }
    .erl-step { flex: 1; min-width: 56px; text-align: center; }
    .erl-step-dot {
        width: 28px; height: 28px; border-radius: 50%; margin: 0 auto 6px;
        display: flex; align-items: center; justify-content: center;
        font-size: 0.78rem; font-weight: 700; border: 2px solid #CBD5E1; color: #94A3B8; background: #FFF;
    }
    .erl-step.done .erl-step-dot { border-color: #0066CC; background: #0066CC; color: #FFF; }
    .erl-step.active .erl-step-dot { border-color: #0066CC; color: #0066CC; background: #FFF; }
    .erl-step-label { font-size: 0.62rem; font-weight: 600; color: #94A3B8; line-height: 1.2; }
    .erl-step.active .erl-step-label, .erl-step.done .erl-step-label { color: #0066CC; }
</style>
<div class="erl-stepper">
    @for (var i = 0; i < steps.Length; i++)
    {
        var stepNum = i + 1;
        var css = stepNum < Model ? "erl-step done" : stepNum == Model ? "erl-step active" : "erl-step";
        <div class="@css">
            <div class="erl-step-dot">
                @if (stepNum < Model)
                {
                    <i class="fas fa-check" style="font-size:0.7rem;"></i>
                }
                else
                {
                    @stepNum
                }
            </div>
            <div class="erl-step-label">@steps[i]</div>
        </div>
    }
</div>
